# PostgreSQL URL injected by runtime.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP listen port.
PORT=8080

# Optional fallback listen port used only when PORT is unset.
APP_PORT=8080
